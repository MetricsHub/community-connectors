#MetricsHub Configuration
otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: "src/main/connector"
resources:
  localhost:
    attributes:
      host.name: localhost
      host.type: oob
    protocols:
      emulation:
        snmp:
          directory: src/it/resources/MIB2/emulation/snmp
    connectors: [ +MIB2 ]