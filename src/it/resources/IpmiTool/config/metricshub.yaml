otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: "src/main/connector"
resources:
  anyHost:
    attributes:
      host.name: anyHost
      host.type: oob
    protocols:
      emulation:
        ipmi:
          directory: src/it/resources/IpmiTool/emulation/ipmi
    connectors: [ +IpmiTool ]