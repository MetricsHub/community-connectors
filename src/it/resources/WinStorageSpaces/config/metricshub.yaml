#MetricsHub Configuration
otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: src/main/connector
resources:
  localhost:
    attributes:
      host.name: localhost
      host.type: windows
    protocols:
      emulation:
        wmi:
          directory: src/it/resources/WinStorageSpaces/emulation/wmi
    connectors: [ +WinStorageSpaces ]