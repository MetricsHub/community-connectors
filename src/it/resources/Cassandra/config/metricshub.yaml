#MetricsHub Configuration
otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: "src/main/connector"
resources:
  localhost:
    attributes:
      host.name: localhost
      host.type: linux
    protocols:
      emulation:
        jmx:
          directory: src/it/resources/Cassandra/emulation/jmx
    connectors: [ +Cassandra ]