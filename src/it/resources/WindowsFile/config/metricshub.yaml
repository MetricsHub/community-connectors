#MetricsHub Configuration
otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: "src/main/connector"
resources:
  localhost:
    attributes:
      host.name: localhost
      host.type: win
    protocols:
      ssh:
        username: user
        password: pass
        timeout: 240
    additionalConnectors:
      WindowsFile:
        variables:
          matchPath: "C:\\Users\\Administrator\\Desktop\\*.txt"
          contentPattern: "Exception|Error|Failed|ec-win-01"