#MetricsHub Configuration
otel:
  otel.exporter.otlp.metrics.protocol: noop
patchDirectory: src/main/connector
resources:
  localhost:
    attributes:
      host.name: localhost
      host.type: linux
    protocols:
      emulation:
        jdbc:
          directory: src/it/resources/MariaDB/emulation/jdbc
    connectors: [ +MariaDB ]